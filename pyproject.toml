[project]
name = "basketball-box-score-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.32.5",
    "opencv-python",
    "numpy",
    "torch",
    "torchvision",
]

[project.scripts]
hoopstats = "hoopstats.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hoopstats"]